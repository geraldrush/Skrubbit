{
  "name": "skrubbit-reminders",
  "private": true,
  "version": "0.1.0",
  "description": "Daily cron that refreshes the eTenders mirror and asks the Skrubbit app to send deadline reminders and new-advert alerts",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  }
}
